# n8n GitHub Sync Workflow

This workflow shows how n8n can automatically trigger ProuMind ingestion through the generic webhook endpoint.

## Goal

Automatically sync GitHub repository knowledge into ProuMind.

Supported source types:

```txt
github_issues
github_pulls
url